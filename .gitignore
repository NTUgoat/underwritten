# Raw SEC corpus — large, reproducible from manifest, never committed
data/raw/
*.duckdb.wal

# Python
__pycache__/
*.py[cod]
.venv/
venv/
.pytest_cache/
.ruff_cache/
*.egg-info/

# Env
.env
.env.local

# Workflow scratch
_wf.json

# OS
Thumbs.db
.DS_Store

# Scratch artifacts left by research subagents — never part of the study
ct.json
tm.json

# Runtime lock, not part of the study
data/derived/corpus_build.lock
